---
unit: b1-u8
book: 1
chapters: ch11 (a/b)
segment: docs/characters/_segments/05-b1-ch11.md
files: ch11a.magium, ch11b.magium
patterns: P5, P6, P7
---

# Unit B1-U8 — Book 1, Chapter 11 (both routes)

## Mode: first real pass, on the highest-consequence file in the project

`ch11a` — 16,805 words, 50 scenes, **0.9% rewritten**. `ch11b` — 11,795 words, 34 scenes, **0.2%
rewritten** (a single word: `zip code` → `district`).

**These two files are Book 1's finale, and they set the flags that the rest of the trilogy reads.**

## The echo surface — read this carefully

`ch11a` sets `v_ch11_romance_rose`, `v_ch11_saved_rose`, and `v_ch11_saved_slave`.

- `v_ch11_saved_rose` is read in **26 files across Books 2 and 3** — the largest echo surface in the
  project.
- `v_ch11_romance_rose` is read in `b2ch2`, `b2ch7`, `b2ch9a`, `b2ch9b`, `b3ch10a`, `b3ch10b`, and is
  re-set in `b3ch3a`. On `== 1` Barry and Rose become physically intimate at the end of this chapter;
  on `== 0` they do not.
- `v_ch11_saved_slave` is read in six files including `b3ch1`.

**Books 2 and 3 are being rewritten concurrently by other masters right now.** You may not edit their
files. Anything you change about what these outcomes *mean emotionally* must be reported as a
`CROSS-FILE` item naming the affected books, so the operator can inform them. Be conservative: prefer
deepening how an outcome is portrayed over changing what it implies.

## `ch11a` — the town chapter

Content: arriving at Thilias and Rose's family estate; her siblings; the slave girl Ella and Suzie; the
decision about Ella's future; Rose bringing muffins to Barry's room; the romance scene or its decline;
then the morning departure to the Beggar's district clinic.

**P5 is your primary target.** The romance branch is gated on `v_ch6_flirt` having been 2, 3, 4, or 6,
then splits again on `v_ch11_romance_rose`. Four inbound flirt values × two outcomes. Currently
`Ch11a-Feelings` has only **two** variants (`v_ch6_flirt == 2` and everything else) — so three distinct
flirt histories collapse into one identical conversation. That is flattening on the most emotionally
loaded scene in the book.

You cannot fix it by adding an `#if` (forbidden — Master Contract §3). Fix it by rewriting the prose
*inside* the existing two variants so each reads as a genuine continuation, and report the structural
limitation under `UNRESOLVED`.

**P7** — the intimacy scene in `Ch11a-Racing2` / `Ch11a-Feelings2` was written with real physical
specificity (herb-smelling hands, small calluses, the candle burning low) and is among the better prose
in the book. **Do not weaken it.** The rest of the chapter — Ella's rescue, the negotiation over her
staying, the sibling scenes — is comparatively unstaged. Work those.

**P6** — Ella's storyline resolves and the chapter immediately jumps to the next morning. A freed slave
choosing to stay deserves a landing pad.

## `ch11b` — the clone fight

Content: Memphir's mages ambushing the party in the marketplace; hundreds of illusory duplicates; the
reflection trap that kills Barry outright below 4 premonition; Arraka identifying the spell.

**P1** — the premonition gate is a pure stat check with instant death on failure. Verify the passing
route reads as Barry *reasoning* about the aura wobble Arraka describes, not as a number clearing a bar.

**P4** — Arraka solves the puzzle. Audit whether the party contributes. Reference: `ch6`'s
`Ch6-Win-fight`.

**P8** — the `zip code` fix was applied here; the rest of the file was never swept. Arraka's voice is
where anachronism hides, because her register is deliberately modern-sounding. The test is whether a
Varathian would say it aloud.

**P6** — a death route exists (`v_premonition < 4`). Check the surviving route gives the near-miss weight.

## Do not

- Do not add, remove, or re-nest any `#if` block. Machine-checked; Master Contract §3.
- Do not change any `choice(...)` label. Sweep S1 has already run.
- Do not weaken the `ch11a` intimacy scene.
- Do not edit `ch10`, any `b2ch*`, or any `b3ch*` file. Escalate flag-meaning changes as `CROSS-FILE`.
