---
unit: b3-u4
book: 3
chapters: ch04 (a/b)
segment: docs/characters/_segments/12-b3-ch03-04.md
files: b3ch4a.magium, b3ch4b.magium
patterns: P3, P5, P1
---

# Unit B3-U4 — Book 3, Chapter 4 (both routes)

## Unit focus

Both routes of chapter 4 (~28.2k words). **You are the origin point of the widest flag in Book 3.**

Both your files **set `v_b3_ch4_billy`**, which is subsequently read in **`b3ch5a`, `b3ch5b`,
`b3ch8a`, `b3ch9a`, `b3ch10a`, `b3ch10c`** and re-set in `b3ch6a`, `b3ch6b`, `b3ch7a`, `b3ch8b`,
`b3ch11a`. That is a reach of eleven files — the most consequential decision surface in the book, and
it starts here.

Whatever `v_b3_ch4_billy` represents, **the emotional weight the player invests in it here determines
whether six downstream files land.** Read every `#if` block keyed on it across your two files, and
establish the register precisely. Then record in your `CONTINUITY EXIT STATE` exactly what each
route value means emotionally — not just mechanically. Six other writers depend on that line.

`b3ch4a` also reads `v_ch11_saved_rose`; `b3ch4b` sets `v_ch11_saved_rose` and reads
`v_b3_ch4_billy`.

**Primary target — P3 residue.** `b3ch4b` runs **2.25 choices per scene** (90 choices / 40 scenes) —
one of the highest densities in Book 3 and a sweep S1 priority target. By the time you receive it, S1
has replaced sanctioned clown labels. Your job is the aftermath, and it is substantial:

For every choice S1 touched, verify in the target scene:
1. the **echo line** repeats the new label verbatim
2. the **NPC reaction** still lands against the *new* line
3. the reaction does not reference a joke that no longer exists

```sh
grep -n '^choice(\|^ID: ' data/en/b3ch4b.magium
```

**Secondary — P5.** Diff the routes and decide deliberately where they should diverge:

```sh
diff <(grep '^ID: ' data/en/b3ch4a.magium) <(grep '^ID: ' data/en/b3ch4b.magium)
```

**Secondary — P1.** Any combat or problem-solving beat must resolve through Barry reading a mechanical
constraint, not a large stat number. Heavy device load costs him physically — torn muscle, nosebleed,
shaking hands.

## Do not

- Do not change any `choice(...)` label. S1 owns labels; you own the prose around them.
- Do not edit `b3ch5*`, `b3ch6*`, `b3ch8*`, `b3ch9*`, `b3ch10*`, or `b3ch11a` — report as `CROSS-FILE`.
