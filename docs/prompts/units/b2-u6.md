---
unit: b2-u6
book: 2
chapters: ch08–ch09 (ch09 a/b)
segment: docs/characters/_segments/08-b2-ch06-08.md, docs/characters/_segments/09-b2-ch09-10.md
files: b2ch8.magium, b2ch9a.magium, b2ch9b.magium
patterns: P5, P7, P1
---

# Unit B2-U6 — Book 2, Chapters 8–9

## Unit focus

Three files spanning a chapter boundary, with `ch9` splitting into two routes. Read **both** segment
docs listed above — your unit straddles the `06-b2-ch06-08` / `09-b2-ch09-10` seam.

**`b2ch8` is the densest scene count in Book 2: 60 scenes across 16.8k words — ~280 prose words per
scene, the lowest average in the book.** Many short scenes is a strong Pattern 7 signal: scenes that
brief are usually stripped of terrain, weather, and physical business, functioning as routing nodes
rather than dramatised moments. Go through all 60 and stage the thin ones.

**Primary target — P5.** This unit is flag-heavy in both directions:

- `b2ch8` sets `v_b2_ch8_energizer` (read in `b2ch10a` **and Book 3 `b3ch11a`** — crosses the book
  seam, escalate if your edits change its implications) and reads `v_b2_ch1_interfere`,
  `v_b2_ch5_scimitar`, `v_second_book_purchased`
- `b2ch9a` reads `v_b2_ch1_puzzle2`, `v_ch11_romance_rose`, `v_ch11_saved_rose`
- `b2ch9b` sets `v_b2_ch2_deal`, `v_b2_ch1_interfere`, `v_ch11_saved_rose`, `v_b2_ch8_energizer`,
  `v_second_book_purchased` and is the **smallest file in Book 2** (9.4k words, 26 scenes)

`b2ch9a` reading `v_ch11_romance_rose` means Rose's register here depends on a Book 1 outcome. On the
`== 1` route they were intimate at the end of Book 1; on `== 0` they were not. Both variants must
read correctly. This is the single most likely flattening failure in your unit.

**Sweep the route twins.** Diff scene IDs between `9a` and `9b` and decide deliberately where they
should diverge:

```sh
diff <(grep '^ID: ' data/en/b2ch9a.magium) <(grep '^ID: ' data/en/b2ch9b.magium)
```

**Secondary — P1.** `v_b2_ch8_energizer` sounds device-related. Wherever the stat booster or an
energising artifact drives an outcome, the win must come from Barry reading a mechanical constraint,
and heavy load must cost him physically.

## Do not

- Do not change any `choice(...)` label. Sweep S1 has already run.
- Never merge, reorder, or collapse an `#if` keyed on `v_second_book_purchased` — commercial gate.
- Do not touch `b2ch10a`, `b2ch1`, `b2ch5*`, or any `b3ch*` file.
