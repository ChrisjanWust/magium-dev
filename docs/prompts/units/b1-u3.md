---
unit: b1-u3
book: 1
chapters: ch04–ch05
segment: docs/characters/_segments/02-b1-ch04-06.md
files: ch4.magium, ch5.magium
patterns: P4, P5, P7
---

# Unit B1-U3 — Book 1, Chapters 4–5

## Mode: mixed — QA `ch4`, deepen `ch5`

`ch4` is the **most heavily rewritten file in Book 1** (26% of prose lines). `ch5` received 9%.

**What was already done in `ch4`:** all four Cutthroat Dave combat branches were rewritten from raw
draft prose into grounded, physical, tactically-specific writing —
- `v_strength == 3`: a fifty-pound boulder crushing through the dagger enchantments, shattered ribs
- `v_agility == 3`: vector management, exploiting tracking lag around blind spots
- `v_reflexes == 3`: deducing the daggers run an automated proximity intercept, then baiting them into
  friendly fire
- the low-stat failure branch: dagger wounds, then Eiden's silent pillar of white flame

Plus all six `Ch4-Defeat-kill` / `Ch4-Defeat-spare` variants and Eiden's exits. **Leave that logic
intact; deepen only.**

**What was NOT done in `ch4`:** Eiden's *introduction* and the negotiation of the deal. That is the
book's most important Pattern 4 surface and it was never audited. Read it against `ch6`'s
`Ch6-Win-fight` / `Ch6-Eiden-leaves`, which are the reference implementation for handling him: he
supplies asymmetric dread, never a bailout, and he speaks conversationally mid-violence because only the
other side is fighting for its life.

**What was done in `ch5`:** the Rose rescue-versus-ignore divergence. On the ignore path Rose now gives
her genuine relief to Daren and treats Barry with polite, razor-edged skepticism; in `Ch5-Stead` she says
aloud that most contestants would have watched her die. On the rescue path she meets Barry's eyes first.
This is the Pattern 5 reference implementation.

**What was NOT done in `ch5`:**
- **`Ch5-Creator`** — a long lore dump on the Creator, the golden fox, and food conjuration. Pure
  white-room exposition. Rose's bedridden-father motivation was added here, but the surrounding
  delivery was not staged.
- **The Clive confrontation** (`Ch5-Monster`) — three mages call Kate a banshee, Rose steps in front of
  a blade, the tournament ward fires, Kate impales a man's shoulder. Never audited. Check Pattern 6:
  does a man being banished and another being maimed get any aftermath?
- **`Ch5-Reckless` / `Ch5-Heroism`** — Kate blushes for the first time, Rose compares them to her
  siblings, Daren announces Barry is smitten. A key P2 beat, untouched.

## Priority targets

1. **P4** — Eiden's introduction in `ch4`. Highest-value single audit in your unit.
2. **P7** — `Ch5-Creator` and the `ch4` opening watch conversation. `ch4` averages 445 words per scene,
   the second-highest in Book 1; long scenes hide floating-head exposition.
3. **P6** — the Clive scene's casualties, and Eiden's departures in `ch4`.
4. **P5** — `ch4` sets `v_ch4_ending` (six values), `v_ch4_dave_defeat`, `v_ch4_spared_dave`;
   `ch5` reads all of them and sets `v_ch5_ignore_scream`, `v_ch5_goblins`. Verify the six endings
   produce six different emotional registers in `ch5`'s opening, not six routes into one scene.

## Do not

- Do not rewrite the four `ch4` combat branches from scratch. They are the project's reference for
  Pattern 1. Deepen only.
- Do not add, remove, or re-nest any `#if` block. Machine-checked; Master Contract §3.
- Do not change any `choice(...)` label. Sweep S1 has already run.
- Do not edit `ch3`, `ch6`, or any later file — report as `CROSS-FILE`.
